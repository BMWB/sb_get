import 'ext.dart';

void main() {}

//a 模块入口
@pragma('vm:entry-point')
void greetingMain() {
  // startSetting(greetingPageConfig);
}

//b 模块入口
@pragma('vm:entry-point')
void feedbackMain() {
  // startSetting(feedBackPageConfig);
}
